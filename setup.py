from setuptools import setup, find_packages

setup(
    name='ml-research-papers',
    description='Project for digesting info and performing experiments with respect to given ML research papers',
    author='Maher',
    packages=find_packages()
)